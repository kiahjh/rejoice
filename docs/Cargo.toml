[package]
name = "docs"
version.workspace = true
edition.workspace = true

[dependencies]
maud = "0.27.0"
rejoice = { path = "../rejoice" }
tokio = { version = "1.48.0", features = ["full"] }
pulldown-cmark = "0.13.0"
html-escape = "0.2.13"

[build-dependencies]
rejoice = { path = "../rejoice" }
